public class Access_Specifiers {
    public static void main(String[] args) {
        // 1. public -> Is accessible outside class, outside the program, outside the package.

        // 2. private -> Only accessable inside that specific class.

        // 3. default ->  Is accessable outside class, outside the program (same package), not accesible ouside 
        //package.
        
        //4. protected ->  Is accessable outside class, outside the program, can be accessible outside class but 
        //through child class without child class it cannot be accessed.

        // There are many non access modifiers " static, abstract, synchronized, native, volatile etc. "

        
    }
}
